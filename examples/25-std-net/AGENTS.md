# std.net example

Run `qz build`. Start `std_net_example.exe server` on Windows or
`./std_net_example server` on Linux. Run the same binary without arguments in
another terminal. Server handles one HTTP request, then exits.
