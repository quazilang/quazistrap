# Example: 10-bitwise

Demonstrates bitwise operators in Quazilang.

## Running

```bash
qz build -o bitwise
./bitwise
```

## Features shown

- `&` (bitwise AND)
- `|` (bitwise OR)
- `^` (bitwise XOR)
- `<<` (left shift)
- `>>` (right shift)
