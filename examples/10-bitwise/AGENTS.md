# Example: 10-bitwise

Demonstrates bitwise operators in Void.

## Running

```bash
void build -o bitwise
./bitwise
```

## Features shown

- `&` (bitwise AND)
- `|` (bitwise OR)
- `^` (bitwise XOR)
- `<<` (left shift)
- `>>` (right shift)
