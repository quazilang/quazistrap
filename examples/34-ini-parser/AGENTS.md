# INI parser example

File round-trip consumer for the source dependency in `../33-ini-library`. `qz run` reads `input.ini` and writes ignored `output.ini`; the built binary also accepts optional input and output paths.
