# Example: 11-conditional-branches

Demonstrates `else if` chains in Quazilang.

## Running

```bash
qz build -o elseif
./elseif
```

## Features shown

- `if` / `else if` / `else` chains
- Multiple conditions evaluated in order
