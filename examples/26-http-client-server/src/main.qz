import std.io;

fn main() i32 {
    io.println("HTTP client/server example");
    io.println("1. Start server: qz run --bin http-server");
    io.println("2. In another terminal: qz run --bin http-client");
    ret 0;
}
