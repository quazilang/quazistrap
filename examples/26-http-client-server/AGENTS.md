# std.net example

Run `qz build`. Start `http-client-server.exe server` on Windows or
`./http-client-server server` on Linux. Run the same binary without arguments in
another terminal. Server handles one HTTP request, then exits.
