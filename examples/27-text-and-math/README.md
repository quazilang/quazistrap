# 27-text-and-math

Builds a small delivery estimate from Unicode text, checked numeric parsing,
string cleanup, and dependency-free `std.math` calculations.

```sh
qz run
```

The example demonstrates practical primitive and math APIs. Owned `String`
values are released automatically at scope exit.
