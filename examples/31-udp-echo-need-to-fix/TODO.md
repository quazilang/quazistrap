# TODO

- `UDP send failed` occurs when running `client.qz` against the `server.qz`. The `UdpSocket.send_to` intrinsic or wrapper in `net.qz` seems to fail to send data properly or the address resolution is failing. Needs to be debugged.
