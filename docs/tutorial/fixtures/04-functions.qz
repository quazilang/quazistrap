fn clamp(value: i32, low: i32, high: i32) i32 {
    if (value < low) { ret low; }
    if (value > high) { ret high; }
    ret value;
}

fn main() i32 {
    const bounded = clamp(12, 0, 10);
    const double: fn(i32) i32 = |value| value * 2;
    if (double(bounded) == 20) { ret 0; }
    ret 1;
}
