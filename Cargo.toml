[package]
name = "void"
version = "26.0.1"
edition = "2024"
license = "0BSD"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
